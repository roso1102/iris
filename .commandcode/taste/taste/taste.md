# Taste
- Prefers discussion and evaluation before action: when a new tool or approach is proposed, wants an analysis of relevance, trade-offs, and options first (explicitly: "discuss, don't act"), not immediate implementation. Confidence: 0.95
- For multi-file code changes, wants a formal plan → approval → execute gate: scope the codebase, check dependents to ensure nothing breaks, present the plan (what exists, what's missing, files to change, why nothing breaks, verification steps), then wait for explicit approval before writing any code. The plan is the bridge between evaluation and implementation — do not proceed from "worth doing" to coding without the intermediate plan-and-approve step. Confidence: 0.85
- When presented with an externally-sourced implementation proposal (recommendations pasted from another tool/person/doc), wants an honest critical evaluation before any implementation — explicitly "be honest" signals a candid do/skip/defer verdict per item with reasoning, separating what's worth doing now from what's overkill for the current phase, rather than blanket endorsement of the whole plan. Confirmed by follow-through: after the verdict, the user instructed implementation of exactly the recommended subset ("implement 2 and 3"), skipping the item flagged as overkill for the dev phase. Reconfirmed with "what do you think of this?" on a pasted code-level rate-limiting alternative to a GCP quota restriction — again answered with a do/skip verdict (skip: premature hedge, add retroactively if a real rate limit surfaces). Confidence: 0.85
- For security-sensitive changes (org-policy overrides, IAM/role grants, anything touching org-wide settings), wants an explicit risk assessment before proceeding: "should i do this or stuff will break?" — i.e., whether the step can break anything, its blast radius (project-scoped vs org-wide), who can exploit the relaxation, whether the user even holds the required role, and a clear recommendation on whether to do it themselves or hand the step to the org admin. Confidence: 0.7
- Works on the same project concurrently with multiple AI coding assistants (e.g., Antigravity and Command Code) and actively seeks ways to share context between them rather than re-explaining. Confidence: 0.9
- Expects the assistant to commit and push changes to the GitHub remote as part of finishing work (said "by the time you push changes to github", and again "update github commit and push"), treating the remote repo as the synchronization point for the user's other tools/assistants — so manual steps the user defers ("ill do step a and step b later") can be done later against an up-to-date repo. Confidence: 0.75
- Endorsed a single living handoff file (`CONTEXT.md`) as the shared source of truth across AI assistants: it tracks only the delta — current phase, binding decisions, session log, gotchas, open questions — while spec docs (README/SRS/ACTIONPLAN) stay untouched, and tool-specific stubs (`COMMANDCODE.md`/`AGENTS.md`) just point to it. Agents are expected to append to the session log and respect "Key Decisions." Confidence: 0.8
- Wants the cross-agent context bridge to be automatic, not manual: expects the assistant to auto-read the shared context at session start and to update the living context file's session log on its own when work completes — zero upkeep on the user's side (asked explicitly: "will you now automatically read context? and automatically update..."). Reconfirmed: after a completed session the user prompted "did you update context.md and the handoff", expecting the end-of-session update (current state, session log, gotchas) to have happened unprompted. Reconfirmed again: user checked "is the context.md update automaticaly tillnow or did you forget" — the assistant had forgotten and had to fix it, further proving the user expects this to happen without prompting. Confidence: 0.95
- Wants shared context files to be strictly governed: prefers a fixed, numbered template with explicit rules (frozen spec sections, append-only session log, no reformatting) mandated through the auto-loaded agent instruction files so concurrent agents can't restructure it; template changes should be proposed to the user, never made unilaterally (explicitly: "make a fixed template and mandate it so it's strict"). Confidence: 0.9
- When asking for the plan or next step, wants a direct, concise answer — "just tell me"/"just tell, dont act" signals to skip elaborate options/analysis and give the single concrete first action (e.g., "create the GCP project, enable the APIs") without taking any action, and to clearly separate what's already completed ("what's already over?") from the single next step. Status/verification questions ("is phase X completely over and tested?") expect the level of verification to be named precisely — deployed vs unit-tested vs integration-tested — and a phase must never be declared "over and tested" while its integration/end-to-end tests are still pending. Likewise, "is this how it is supposed to be or what happened was wrong, tell me" expects a direct, unambiguous correctness verdict — "the code is correct, but the tests bypassed Docling" — not a hedged maybe, and the answer must clearly separate what's fine from what's incomplete/gapped. Confidence: 0.85
- When the user must run manual cloud/infra steps themselves (gcloud, terraform, shell scripts), wants them explained "one by one, step by step, in an easy way": simple numbered steps, exact copy-paste commands, environment-specific adaptations (e.g., Windows/Git Bash, notepad), what output to expect, and explicit checkpoints where they pause and report results before continuing (asked explicitly: "tell me one by one, step by step how to do these things... in easy way"; confirmed in practice — reported "step 0 is over" and asked "whats step 1 now?" instead of proceeding). Confidence: 0.9
- Reports manual-step results/failures by pasting the full raw terminal output verbatim — deprecation warnings, repeated-run output, and all — without filtering or summarizing; expects the assistant to extract the error from the paste and diagnose (e.g., re-pasted the same failing `setup_firebase.sh` output after logging in, letting the assistant diff it against expectations; pasted the entire `deploy.sh` gcloud output including the gcloudignore notice and the interactive "enable API? (y/N)" prompt rather than answering the prompt himself, expecting the assistant to explain what to type; pasted the raw `gcloud.builds.submit PERMISSION_DENIED` + "ingestion-worker build failed" output verbatim with only a terse "finished successfully" lead-in, letting the assistant diagnose the missing Cloud Build permission; pasted the full pip dependency-resolution failure output verbatim — download lines, pip upgrade notices, and the entire available-versions list — so the assistant could spot that the `google-cloud-run>=1.16` pin could never resolve; pasted a build tail containing only harmless pip warnings (root-user warning, pip upgrade notice) with no error and no lead-in, expecting the assistant to judge that the build actually succeeded and verify via gcloud). Confidence: 0.95
- Has beginner-level terminal/CLI fluency: doesn't know where to type commands or how directory navigation works (asked "where is cd infra, like where to type this" and again "where to run step 6? in cmd or powershell or elsewhere?"). Instructions must assume zero CLI literacy — spell out that commands go in the same terminal window, name the specific shell program to use (e.g., Git Bash for `.sh` scripts vs CMD/PowerShell), explain the current-folder concept, and offer low-friction alternatives like opening a command prompt from Explorer's address bar instead of typing paths. Confidence: 0.9
- Prefers pragmatic, MVP-scoped choices over state-of-the-art options: keeps the verified, cheaper 768-d embedding model for the MVP and defers the higher-quality 3072-d upgrade to a future phase, explicitly marked "not in MVP scope" rather than building for it now. Confidence: 0.7
- Prefers keeping data within their own infrastructure/VPC bounds over third-party SaaS solutions, even when the SaaS option is simpler and faster for an early-phase setup — data locality takes priority over convenience. Evidenced by choosing a GCE VM for Qdrant (data stays in `iris-vpc`) over the recommended Qdrant Cloud free tier, accepting the provisioning overhead to avoid data leaving their network. Confidence: 0.7
- Wants technical decisions and their future upgrade paths written into the canonical spec docs (e.g., SRS.md) as explicit notes — including why the choice was made and what a future migration would require — so the rationale is preserved for later phases. Confidence: 0.7
- When a dependency resolution fails during build, wants the assistant to verify every version pin in the requirements files against what actually exists on PyPI (checked `qdrant-client` and `docling` after fixing `google-cloud-run`), not just fix the one surfaced error — anticipating the next landmine before another slow deploy cycle. Confidence: 0.8
- Prefers exact/range-pinned dependency versions that provably resolve (pins `google-cloud-run==0.16.*` to the real latest instead of a never-available `>=1.16`), and treats requirements pinned against nonexistent versions as a bug to root-cause. Confidence: 0.8
- Delegates execution to the agent without step-by-step approval ("start task 1.0"/"start task 1.1") but expects the agent to finish by reporting the concrete actions the user must do on their end (e.g., console/GCP steps the agent can't perform) — an explicit human-side handoff list after autonomous work, and only surfaced when something is actually required (asked: "tell me what to do from my end if required"). When given commands to run manually (e.g., `terraform apply`), prefers the agent to just run them itself ("can you do it for me") rather than running them on their end — including commands handed to them by a third-party admin (e.g., manoj's `gcloud organizations add-iam-policy-binding` grant): the user forwards the command and asks "can you do the pub sub thing enabling," expecting the agent to execute it, verify the grant took effect, and drive the follow-up to completion itself. Confidence: 0.95
- Expects the assistant to install required CLI tooling itself rather than merely instructing the user to — was surprised Terraform/Firebase weren't installed ("did not u install them?"), and expects the assistant to also diagnose and fix environment issues (e.g., winget PATH quirk) rather than handing those off; installation should prefer whatever package managers are already available (winget, npm). Confidence: 0.6
- When guiding through manual steps, does not want to redo anything already completed: when the user says they've already done a step ("i have done this already once"), verify the actual state (e.g., check `gcloud auth list` / `gcloud config get-value project`) and skip ahead, rather than re-instructing or asking them to repeat it. Confirms completion tersely ("done updatef terraform thing", "done iot's done" after finishing a manual browser step) instead of pasting requested output — the assistant should read/verify the actual state itself (e.g., via gcloud) and fix discrepancies rather than demanding pasted output. Confidence: 0.8
- Proactively audits generated infra config against their real-world setup before applying: flags region, billing currency, and label values that don't match what they actually have (e.g., "currency is inr", "owner tag say iris-team, i havent put that anywhere") and expects defaults to be explainable/configurable rather than blindly trusted. Confidence: 0.6
- Asks whether a separate/redundant CLI step is actually needed before running it (e.g., "should i do terraform refresh or just apply?") — wants confirmation that they can skip redundant commands and use the leanest valid sequence. Confidence: 0.5
- When a task depends on a third party with elevated access (e.g., an org admin like "manoj"), wants the complete, exhaustive list of every role/permission required for the entire remaining flow gathered up front in one single request — explicitly "i can't ask manoj again and again, if there are any other roles required please tell now itself" — so the other person is only asked once, with zero iterative permission discovery. Confidence: 0.8
- When a recommended approach carries a security/policy tradeoff, wants the alternatives that avoid the tradeoff entirely before deciding — bypassing the blocked component, replacing the mechanism, deferring, or re-creating the setup differently (asked: "without the tradeoff what's the other option, should i re create this in a different way") — and expects them honestly ranked, not just reassurance that the tradeoff is acceptable. Confidence: 0.6
- After options are ranked, wants the recommended one explained in full, plain-language detail before committing: exact step-by-step actions, what happens afterwards, what is concretely given up (the real risk, not jargon), reversibility, and a bottom-line verdict — including recalling and re-explaining a previously proposed option combination when asked ("explain the option 1", "you suggested some option 1 + 3 approach, what was that?", again "what ideas you suggested again"). Confidence: 0.8
- When a security tradeoff is presented, wants to know how mature/enterprise organizations solve the same problem before accepting a dev-phase compromise — i.e., seeks the industry-standard least-privilege fix (e.g., appending Google-managed service-account domains like `gserviceaccount.com` to the org's allowed-domains list) over blanket project-level "Allow all" exemptions, so org-wide controls stay intact (asked "how do big companies avoid..." after the override's tradeoff was explained, and again "is that how big companies in production do this?" when reviewing privileges before tests). Confidence: 0.7
- When a deploy/infra error is reported, wants the root cause fixed AND all related gaps proactively closed in the same pass so no further issues surface on subsequent runs — explicitly "identify the issue and fix it and make sure no other issues come up": expects the agent to audit same-class permissions and sibling resources (e.g., the compute SA's objectViewer on all GCF staging buckets + writer on the gcf-artifacts repo after one build-step permission error) rather than letting failures surface one at a time across repeated deploy cycles. Confidence: 0.7
- Wants exhaustive, complete inventories of every permission/grant the agent has enabled — explicitly "give me a list of permissions... all of them, dont miss a single" — expecting a full layer-by-layer dump (project/org/billing IAM, service accounts, Cloud Run, Pub/Sub topics and subscriptions, Artifact Registry, GCS buckets, Eventarc, org-policy overrides) with not a single binding omitted, rather than a summarized or partial list. Confidence: 0.8
- Gates the next phase's tests on a security checkpoint of the deployed state: before running integration tests (e.g., the PDF ingestion tests), wants confirmation that the system is safe and privileges are correctly/least-privileged assigned, plus a benchmark against production-grade enterprise practice — answered with a direct plain-language audit and honest caveats (dev-phase compromises like a project-level org-policy override called out), not a bare "yes" (asked "is our system safe and with all correct privileges assigned, is that how big companies in production do this?"; then re-requested the full recap with "give me the constraints we are in, how vulnerable we are and what ideas you suggested again" right before the PDF tests). Confidence: 0.7
- Wants configuration values set outside IaC (e.g., Vertex AI quota caps configured manually in the Cloud Console) written down in the repo — a comment in the Terraform files or a line in CONTEXT.md Gotchas — explicitly so that months later a mysterious failure (e.g., `429 RESOURCE_EXHAUSTED`) can be traced back to the manually-chosen values instead of being re-debugged from scratch ("write down the values you chose somewhere in the repo... Six months from now when your ingestion starts returning 429 RESOURCE_EXHAUSTED and you've forgotten why, you'll thank yourself"). Confidence: 0.8
- Does not want any AI/assistant co-author attribution in git commits — explicitly "dont mention any co author, especially commandcode" — the commit history should look fully human-authored, with no `Co-authored-by` trailers naming the AI assistant. Confidence: 0.9
- Wants deferred or blocked tasks (manual steps, or anything gated on an external condition like a billing-account upgrade) explicitly recorded as a "to do later" item in the shared context file alongside the gotchas, so the task survives the blocker and gets picked up later instead of being dropped ("so updated the gotchas, and add it to do later"). Confidence: 0.5
- When asking where a note/restriction was recorded ("where did you add free trial restriction thing?"), wants a precise pointer to its location — file, section, and line (e.g., "CONTEXT.md §5 Gotchas, line 73") — rather than a vague "in the docs," so they can verify and find it themselves. Confidence: 0.4
- When requesting tests to be run, expects per-test statistical output with quantitative metrics (counts, percentages, crosstabs) and a qualitative accuracy assessment alongside each result — not just pass/fail. The assistant should also surface "possible questions" (known caveats, edge cases, gaps the test doesn't yet cover) so the user can gauge completeness at a glance. Confidence: 0.6
- Expects integration tests to exercise the real production code path rather than bypassing key components with simplified simulations: when reported metrics (VLM %, chunk counts, routing decisions) come from a toy extraction path instead of the actual pipeline, those numbers are misleading and don't answer "is this working correctly?" — tests should route through the same Docling parser, router, and chunker that production uses, or else the test artifact must be explicitly called out as such. Confidence: 0.8
- Prefers GPU acceleration for ML workloads: when local GPU hardware is available (e.g., RTX 5060), expects it to be leveraged for compute-heavy tasks like Docling document parsing rather than defaulting to CPU — explicitly "use gpu for docling." The assistant should detect and upgrade CUDA tooling (PyTorch, drivers) to enable GPU utilization before running the workload. Confidence: 0.85
- Prefers managed GCP infrastructure services over bespoke workarounds for plumbing concerns: when given a choice between Cloud NAT (proper managed service), a pre-baked VM image, or a manual binary SCP install to solve private-egress for Docker pulls, chose Cloud NAT — accepting the extra Terraform setup in exchange for a standard, observable, GCP-native solution rather than a one-off hack. Confidence: 0.65
- Wants every infrastructure provisioning step codified in Terraform for full reproducibility — not just core resources (VPC, subnet, VM) but also networking plumbing like Cloud NAT, so the entire stack is a single `terraform apply` away ("VM creation + Cloud NAT + Startup Script = 1-click deployment"). Confidence: 0.75
- When requesting a security code review, wants a formal penetration-testing-style vulnerability report with structured findings (risk level, vulnerability type, code location, threat explanation, and secure refactor for each issue) covering injection, data leakage, secrets management, race conditions, and DoS vectors — but explicitly does NOT want the assistant to apply any fixes: "DO NOT ACT, ONLY REPORT TO ME IN reply." The report is reviewed and the user decides which findings to address before code changes begin. Confidence: 0.85
- When a vulnerability report is complete and the user is ready to move to remediation, wants a tiered implementation plan first — ranked by criticality (Critical → High → Medium → Low) with gating criteria per tier, each finding mapped to the specific files that will change and a description of the exact approach — and wants to review the plan before any code is modified ("don't act on it"). This is the bridge step between receiving the report and starting fixes: plan, review, then implement. Confidence: 0.7
- Prefers installing dev/ecosystem tooling early in a project's lifecycle to maximize accumulated context over time — "earlier we start, better context it can hold" — rather than deferring tooling setup until the codebase grows large enough to need it. Confidence: 0.7
- Prefers dev tooling installed inside the project's `.venv` rather than globally/system-wide: when the assistant attempted `uv tool install` then `pipx install` (both unavailable), the user asked "do we need it install it in venv?" — confirming that project-scoped pip install is the expected/approved approach over global tool managers. Confidence: 0.5
- When a dev tool has been installed and configured (especially with auto-maintenance hooks like git post-commit rebuilds), expects the assistant to integrate it into its default workflow and use it proactively where appropriate — not treat it as purely on-demand or wait for explicit per-query invocation. The investment in setup implies an expectation of automatic leverage. Explicitly reinforced: "use graphy if required." Confidence: 0.75
- Uses Command Code as their primary AI coding assistant — agent-specific instruction files should target `COMMANDCODE.md`, not `CLAUDE.md`. When tooling auto-generates instructions into an agent stub file (e.g., graphify writing usage rules), it must write to `COMMANDCODE.md`; a separate `CLAUDE.md` alongside it creates confusion and should not exist. Confidence: 0.8
- Prefers end-to-end / integration testing on the actual deployment infrastructure (e.g., Cloud Run workers) rather than fighting local hardware constraints that don't exist in production. Local testing is for unit tests and component smoke tests; full-pipeline verification belongs on the real architecture where GPU, VRAM, and dependencies match production. Confidence: 0.75
- When a test is gated behind an environment variable and skipped by default (e.g., `RUN_VERTEX_LIVE_TESTS=1` for live API calls), wants to run it at least once to confirm it actually passes against real infrastructure — not just accept the skip and move on. The skip is understood as a safety gate, not a reason to never exercise the live path. Confidence: 0.65
- Prefers smoke-testing with the simplest possible input before scaling to realistic workloads: verify the pipeline works end-to-end with a trivial document (e.g., a 2-page blank PDF) first, then progressively test with larger/more complex inputs — explicitly "start with a simple 2 page pdf first, just to check if it's working, dont start with 70 page things." Confidence: 0.7
- Prefers chunked/batched processing for large workloads over monolithic all-at-once runs: when processing large documents (e.g., 70-page PDFs), expects the system to process them in smaller page batches (10–15 pages) rather than feeding all pages into the pipeline at once. Confidence: 0.65
- Seeks "how do big companies manage this?" as a reference point across all technical domains — not just security tradeoffs (already recorded) but also architecture, performance optimization, and system design. Wants to understand the industry-standard playbook before committing to an approach, and expects the answer to be grounded in real production patterns (e.g., Google Document AI / AWS Textract page-level parallelism) rather than speculative ideas. Confidence: 0.755
- Views libraries/dependencies as replaceable components, not permanent commitments: when a dependency becomes a bottleneck, wants a landscape comparison of what production companies use, why alternatives are faster, and the cost/license trade-offs of switching — "what if we kick X out" is a legitimate architectural question, not a complaint. Confidence: 0.6
- Prefers using the latest available model versions over older/superseded ones: when the assistant picked `gemini-1.5-flash-001`, the user explicitly overrode with newer model names ("use gemini-3.5-flash-lite and gemini-3.6-flash, these are latest"). Reconfirmed: user directed upgrading from `gemini-2.0-flash-001` to `gemini-2.5-flash` and `gemini-2.5-flash-lite` across synthesis, query rewriting, and vision OCR roles. The user treats "latest" as the default expectation and will correct older model choices. Confidence: 0.80
- When cloud AI models aren't all available in a single region, prefers splitting routing by capability and latency requirements across regions (e.g., embeddings local to the project region for low latency, VLM calls routed to the region where the needed models actually exist) rather than forcing everything into one suboptimal region or relocating the entire project. Described as the "most elegant long-term" approach and explicitly committed to for implementation ("see Option 3... do this"). Confidence: 0.75
- Is sensitive to Docker build/deploy iteration cycle time and wants the assistant to factor it into approach decisions: when a proposed change might trigger a rebuild cycle, expects an explicit breakdown of where the time goes (apt-get, pip install, model warm, push, deploy) so the iteration cost is clear before committing — rather than discovering the 20+ minute loop through trial and error. Confidence: 0.55
- Prefers defense-in-depth billing protection against cloud overspend: promotional credits consumed first (not bypassed by upgrading), GCP budget alerts with automated kill switches to cut ingestion spend, and ideally credit-card-level hard caps (e.g., ₹500) — wants multiple financial guardrails, not just a single budget alert, and expects a concrete cost breakdown before any billing-account change. After making account changes (e.g., upgrading to paid), proactively verifies the guardrails still hold by walking through the exact money flow — credits vs. budget thresholds vs. card charges — before proceeding with further work. Confidence: 0.80
- Expects fast inner dev loops — sub-minute feedback for code changes, not 20-minute Cloud Build cycles. Prefers the production-engineering workflow: test locally with mocks first (seconds), use env vars for config changes on deployed infra (minutes), and reserve full Docker rebuild + deploy only for package or system-dependency changes. Explicitly directed: "test locally first, then when everything works only the rebuild and then do" — exhaust local validation before a single, definitive deploy, not iterative rebuild-deploy-debug cycles. Confidence: 0.85
- When asking for benchmark/performance data, wants it formatted as a structured technical report with tables, labeled sections, and quantitative metrics suitable for sharing or presenting to others — explicitly "keep it technical and something i can show in a report": timing breakdowns, processing output, infrastructure specs, cost estimates, bottleneck analysis, and optimization impact, not raw log dumps or casual summaries. Confidence: 0.7