# CONTEXT.md — Living Project Context for IRIS

<!-- STRICT TEMPLATE v1 — This file has a fixed structure. -->
<!-- RULES FOR ALL AGENTS (Command Code, Antigravity, any other): -->
<!-- 1. Section headers (## 1. … ## 6.) are FIXED. Never rename, reorder, merge, or add sections. -->
<!-- 2. Sections 1 and 3 are FROZEN (derived from README.md / SRS.md / ACTIONPLAN.md). Only the user may authorize changes. -->
<!-- 3. Sections 2, 5, 6 are updated only when reality changes (work done, new gotcha, new open question). -->
<!-- 4. Section 4 (Session Log) is append-only. Add ONE bullet per session at the bottom. Never edit or delete old bullets. -->
<!-- 5. Keep this file under ~160 lines. Trim only the oldest Session Log bullets if it grows. -->
<!-- 6. Do not reformat the file (headings, bullets, bold markers, the divider). -->
<!-- If you believe the template itself must change, propose it to the user — do not change it unilaterally. -->

---

## 1. What IRIS Is

IRIS is a secure, multi-tenant, spatially-grounded document Q&A platform on GCP. Clients upload dense PDFs (legal filings, gazettes, scanned reports) and ask natural-language questions. Answers carry **pixel-accurate citations** — clickable highlights mapped to exact bounding boxes on source pages. Built serverless-first, CPU-only, cost-capped by a $300 GCP credit. Frontend on Vercel (Next.js); backend on Cloud Run + Qdrant + Firestore + GCS + Pub/Sub + Vertex AI.

## 2. Current State

- **Phase:** Phase 0.0, 1.0, 2.0, 3.0, 3.5, and **4.0 complete**. **Phase 3.0 (Synthesis):** `/query` endpoint with grounded structured citations, `validate_citations()` hallucination guard, FastEmbed BM25 with baked Docker cache (deployed). **Phase 3.5 (Retrieval Hardening):** strict page-boundary chunking (parser splits multi-page elements via `prov` charspans), eval harness uses server `latency_ms` + cached auth token, Cloud Run scaling tuned (ingestion-worker min=0, retrieval-api min=1 — live). Eval after re-ingest: Recall@5 0.830, Page-Recall@5 0.320, MRR 0.262, latency P95 2.9s (true server-side). **Phase 4.0 (Auth & Multi-Tenant Security):** Firebase JWT verification on all user routes (`services/common/auth/`), strict tenant rewrite from JWT claims (anti-IDOR), Firestore security rules deployed live, session CRUD + cascading deletes, signed GCS URLs (IAM signer), per-tenant rate limiting. All four live penetration tests (4-A cross-tenant rewrite, 4-B Firestore rules, 4-C signed URL TTL, 4-D rate limit) PASS. retrieval-api is `--allow-unauthenticated` (app enforces Firebase JWT via `X-Firebase-Token`); ingestion-worker stays `--no-allow-unauthenticated` (Cloud Run IAM).
- **Implemented:** `ModelProvider` abstract class, `VertexAIProvider`. Phase 0.0: 56 Terraform resources. Phase 1.0: Ingestion pipeline (preflight, Docling parser, 4-signal VLM Router with `do_ocr=False` fix and page-level Signal 4, chunker, QdrantChunkStore, PDF splitter, doc hash cache), worker on Cloud Run. **Phase 2.0:** `services/common/retrieval/` (rrf, diversity, search orchestrator). `services/common/ingestion/store.py` upgraded to `iris_chunks_v2` with `tenant_id` mandatory in `get_by_ids()`. `services/retrieval_api/app.py` rewritten as FastAPI with cascading deletes to GCS/Firestore. Ingestion progress moved to Firestore to survive cold starts. CI deployment env vars matched to `deploy.sh`. **Phase 4.0:** `services/common/auth/` (jwt, claims, rate_limit, validation), `firestore.rules` + `infra/firestore_rules.tf`, session CRUD (`POST/GET /sessions`), `GET /documents/{doc_id}/view-url` with 15-min V4 signed URL, `X-Firebase-Token` header for Cloud Run pass-through of Firebase JWTs, eval harness (`scripts/eval_phase2.py`) mints real Firebase ID tokens for the provisioned `eval@iris.local` user, CI Firestore-rules test job added.
- **Next up:** Phase 5.0 — Frontend Integration (Vercel). **Sequencing decided (2026-08-20):** (1) CORS on `retrieval-api` **first** — the only hard blocker; the browser physically cannot call the API without `Access-Control-Allow-Origin`; (2) walking-skeleton core loop — Firebase Auth → `/query` → PDF.js citation panel — golden docs are already ingested under test-tenant, so the demo works with zero upload; (3) `POST /documents/upload` **deferred** until the core loop works end-to-end (upload UI ships disabled with a "docs coming soon" state); (4) polish items deferred — OpenAPI error schemas (401/403/404/429/500), typed `SessionList` schema, CI API-drift check. **CORS implemented in code (Task 5.0a) but NOT yet deployed** — see Session Log 2026-08-20.

## 3. Key Decisions (frozen — do not violate)

- **Serverless-first:** every compute component scales to zero; no idle billing.
- **CPU-only for MVP:** no GPU quota available. All model calls go through a `ModelProvider` interface so GPU swap-in later is a config change (`MODEL_BACKEND` env var), not a rewrite.
- **Tenant isolation at the engine level:** JWT `tenant_id` claim drives Qdrant filter rewriting server-side; never trust client-supplied tenant IDs. Firestore security rules enforce `request.auth.token.tenant_id == resource.data.tenant_id`.
- **Cost-capped by default:** billing circuit breaker + ingestion DLQ built in Phase 0.0, before any product code.
- **Page-wise VLM router:** Docling does layout extraction with bboxes; Gemini Vision is called ONLY for tables, figures, and low-text (<150 char) pages — never on clean text pages. Handles KrutiDev/DevLys legacy Hindi + scanned Devanagari without custom decoders.
- **Stack specifics:** Qdrant self-hosted on GCE VM (`is_tenant=True`, hybrid BM25 + cosine, RRF fusion, diversity/dedup pass); Vertex AI `text-embedding-004` (768-d); Gemini Flash synthesis; Firestore for sessions/history/quotas; GCS tenant-prefixed buckets.
- **Embeddings locked at 768-d for MVP:** `text-embedding-004` @ 768-d is the MVP standard (multilingual, verified live). The 3072-d `gemini-embedding-001` upgrade is explicitly **post-MVP** — it requires a full re-embed + Qdrant collection migration, not a config swap. Documented in `SRS.md` §5.2. Do not plan MVP work around 3072-d vectors.
- **Two query modes:** Standard (fast/free) and user-toggled Deep Search (SLM rewrite + HyDE + Vertex AI Ranking rerank).
- **Licensing:** Docling (MIT) OK; **Marker is banned** (Open-RAIL-M revenue trap). Cohere Rerank v3.5 is deprecating — use `rerank-4-fast` or Vertex AI Ranking API.
- **Milestone checklist (working state, not a locked decision):**
  - [x] Initial specification and roadmap committed & pushed to GitHub.
  - [x] Auto-loaded `.agents/AGENTS.md` and `CONTEXT.md` initialized.
  - [x] Implement `ModelProvider` abstract interface, `VertexAIProvider`, and `MockModelProvider` scaffold (Phase 0.1).
  - [x] Authenticate local gcloud CLI & verify live Vertex AI `text-embedding-004` 768-d embedding call.

---

## 4. Session Log (append-only — one bullet per session)

<!-- Format: `- YYYY-MM-DD · tool · what was done | decisions made | next step` — max 3 lines per bullet -->

- 2026-07-31 · discussion · Set up shared-context files (`COMMANDCODE.md` → `CONTEXT.md` ← `AGENTS.md`). Updated `CONTEXT.md` with complete project state and rules structure. Next: decide whether to start Phase 0.0 GCP scaffolding.
- 2026-07-31 · Command Code · Verified the bridge end-to-end: `COMMANDCODE.md` (Command Code, auto-load) and `.agents/AGENTS.md` (Antigravity, auto-load) both resolve to `CONTEXT.md` as the single source of truth. Confirmed the other agent's rewrite of `CONTEXT.md` preserved all key decisions. Next: decide whether to start Phase 0.0 GCP scaffolding, or Phase 0.1 `ModelProvider` scaffold.
- 2026-07-31 · Antigravity · Authenticated local `gcloud` CLI with GCP project `naturepivot-rag`. Successfully executed live Vertex AI `text-embedding-004` integration test returning 768-d vector. Next: proceed to Phase 1.0 Core Ingestion Worker.
- 2026-07-31 · Command Code · Locked embeddings at 768-d for MVP (`text-embedding-004`, multilingual) and documented the 3072-d `gemini-embedding-001` upgrade as explicitly post-MVP (re-embed + Qdrant migration required) in `SRS.md` §5.2. Updated `CONTEXT.md` §3 with the decision. Next: Phase 1.0 Core Ingestion Worker — Task 1.1 tenant-prefixed GCS buckets.
- 2026-07-31 · Antigravity · Created `BENCHMARK.md` containing the technical RAG evaluation suite (Recall@5, MRR, RAGAS Faithfulness, WER, Latency P95, and comparison baselines). Next: Phase 1.0 Core Ingestion Worker.
- 2026-07-31 · Command Code · Wrote Phase 0.0 (Terraform infra, billing kill-switch function, hello-world services, CI, Firebase script) + Phase 1.0 core (preflight, Docling parser, VLM Router, chunker, ChunkStore memory/Qdrant, worker Pub/Sub app) + 4 unit-test files. 23 tests green; live Vertex AI 768-d test still passes. Folded in corrections: `is_tenant` is not a real Qdrant param, and table/picture elements route to VLM **before** the low-text check. Next: **human handoff** — terraform apply, setup_firebase.sh, deploy.sh, then Phase 1.0 integration tests 1-A…1-H.
- 2026-08-01 · Command Code · Phase 0.0 infra APPLIED: fixed label regex (GCP disallows dots), `aiplatform` API name (not `vertexai`), `roles/aiplatform.user`, VPC connector /28 subnet, Pub/Sub `max_delivery_attempts=5` (min raised), Cloud Run services moved to deploy.sh (images must exist first). 53 resources live incl. VPC/peering/PSC, secrets, bucket, topics, SAs. Billing budget deferred (`enable_billing_budget=false`) — requires Billing Account Administrator/Costs Manager which rohit lacks (manoj has owner). Next: setup_firebase.sh, deploy.sh, Test 0-A.
- 2026-08-01 · Command Code · Phase 0.0 COMPLETE: Firebase linked (org-policy permission fix required `roles/firebase.admin`+`roles/orgpolicy.policyAdmin` granted by manoj), budget connected to `billing-alerts` topic (Console-only step — `billing-<ID>@...` SA never exists; real SA is `billing-budget-alert@system.gserviceaccount.com`), both services built + deployed + healthy, kill-switch function deployed + verified (pushConfig detach via Pub/Sub Python client, 15 iterations to fix runtime deps/proto field names/Cloud Run v2 PATCH failures; Test 0-A passes). 45+ IAM bindings across project/org/billing/run/pubsub/artifact-registry/storage. Known quirks: `max-instances=0` rejected by Cloud Run (Knative requires positive integer), `gcloud` CLI absent from Cloud Functions gen2 Python runtime, `iam.allowedPolicyMemberDomains` org policy required project-level ALLOW override for billing SA. Next: Phase 1.0 integration tests 1-A…1-H.
- 2026-08-04 · Command Code · Security hardening pass: fixed 10 findings across 5 source files — prompt injection sanitization (vertex.py), cross-tenant Qdrant filter (store.py), QA view auth gate (qa_view.py), local dev path traversal (main.py), billing kill-switch fail-fast (billing-kill-switch/main.py), threading.Lock on MemoryChunkStore, image size validation + retry on Gemini Vision, exact model name map, hardcoded GCP_PROJECT removal, empty response crash protection. Added `tests/test_ingestion_security.py` (16 new tests, 28 total green). Installed graphify (368 nodes, 952 edges, 21 communities) with git hooks for auto-rebuild on commit. Moved graphify instructions from `CLAUDE.md` to `COMMANDCODE.md`. Next: Phase 2.0 Vector Store & Retrieval.
- 2026-08-06 · Antigravity · Fixed CI build: resolved test_local_dev_allows_test_docs_path failure with dynamic dummy PDF creation and gated test_vertex_embedding behind RUN_VERTEX_LIVE_TESTS variable. Swapped default region from us-central1 to asia-south1 throughout (models, terraform config, setup script, SRS.md). Next: Phase 2.0 implementation.
- 2026-08-06 · Command Code · Reviewed and updated Phase 1.0 cloud integration test plan: wired GET /memory QA endpoint in app.py, added DLQ pull subscription in pubsub.tf, made QdrantChunkStore collection creation idempotent, corrected preflight DLQ expectations. Ran live Vertex AI 768-d embedding test (PASSED). Next: deploy updated worker to Cloud Run.
- 2026-08-08 · Command Code · Phase 1.0 complete: deployed ingestion-worker v00052-md6 with full pipeline verified end-to-end. Fixed 14 issues across 5 rebuild cycles — Pub/Sub parsing, OpenCV deps (libgl1/libxcb1/g++), TORCH_COMPILE_DISABLE (4× cold start speedup), Qdrant collection idempotency, _crop_bbox bbox clamping, VLM fallback on failure, vertexai SDK, Cloud NAT for Qdrant VM egress, split-routing to us-central1, model names (gemini-2.5-flash). Benchmarked: 121s cold start (2 pages, 10 chunks, 2 VLM calls). Parallel dispatch verified: 2 pages concurrent, 5× projected for 35-page docs. Billing kill switch at ₹500 cap. Next: run 1-A through 1-I integration tests.
- 2026-08-09 · Command Code · Phase 2.0 retrieval pipeline implemented: `services/common/retrieval/` (bm25, rrf, diversity, validator, search orchestrator, models), v2 Qdrant collection schema (iris_chunks_v2 w/ named vectors, binary quant, payload_m=16, keyword indexes), FastAPI retrieval-api (POST /search standard+deep, DELETE /documents, DELETE /sessions), cascading delete, deploy configs updated. `retrieval-api` renamed to `retrieval_api` for Python import. 72/72 unit tests green. Graphify: 878 nodes, 1670 edges, 83 communities. Next: deploy to Cloud Run, run Phase 2.0 integration benchmarks.
- 2026-08-09 · Antigravity · Updated ACTIONPLAN.md with Phase 2.0 non-negotiables (async non-blocking event loop, Cloud Logging search observability, rank_bm25 TF-IDF upgrade) and added Phase 2.5 Empirical Validation & Hardening (RAGAS evaluation, VLM table validation, HyDE/Reranker A/B benchmarks). Next: Phase 2.0 implementation.
- 2026-08-15 · Antigravity · Phase 2.0 core plumbing complete and deployed (ingestion-worker-00066-cwm, retrieval-api-00007-pcz). Must-fix list done: cascading deletes (GCS+Firestore+Qdrant), tenant_id in get_by_ids(), Firestore ingestion state, CI env-var parity, latency_ms logging, PII redacted from Pub/Sub logs, thinking_budget=0 confirmed active. Also: do_ocr=False fixed RapidOCR crash (165 chunks vs 10), Signal 4 upgraded to page-level char count, vlm_calls now a per-doc delta. BM25 hash bug fixed (hash(term) → mmh3). Deferred: FastEmbed BM25 (Task 3.5), deep search reranker (Phase 12), VLM table validation (Task 2.5.1). Next: Phase 3.0 — /query endpoint + FastEmbed BM25 upgrade.
- 2026-08-15 · Command Code · Phase 3.0 Task 3.1 DONE: `synthesize()` now accepts `source_chunks`, uses Vertex structured output (`response_mime_type=application/json` + `response_schema`), maps returned chunk_ids to real `Citation`s, and returns grounded citations instead of `[]`. Updated `Citation` (added `chunk_id`), all providers (`vertex`/`mock`/`gpu`), and tests. Patched `test_retrieval_api.py` to mock GCS/Firestore. Live `test_vertex_synthesis_2_5_flash` PASSED (13.24s). Next: Phase 3.0 Task 3.2 (`/query` schemas).
- 2026-08-15 · Command Code · Phase 3.0 Tasks 3.2 + 3.3 DONE: added `QueryRequest`/`QueryResponse` (answer, citations, mode, latency_ms, chunks_used) and wired `POST /query` (retrieve → `_build_synthesis_context` → `provider.synthesize` via `asyncio.to_thread` → grounded response). Added mock-path `/query` test (145 total non-live tests green). Next: Task 3.4 citation-validation guard.
- 2026-08-15 · Command Code · Phase 3.0 Task 3.4 DONE: added `validate_citations()` in `services/common/retrieval/synthesis.py` (drops hallucinated chunk_ids, overwrites doc_id/page/bbox/snippet from trusted chunks), wired into `/query`. 4 unit tests in `test_synthesis.py`. Deleted `test-docs/` and rewired all Docling integration tests + `main.py` local-dev gate to `trueassort/`. Ran all 8 integration + 4 Vertex + 2 Qdrant live tests — all passed (149 non-live + 6 live green). Next: Task 3.5 FastEmbed BM25.
- 2026-08-15 · Command Code · Phase 3.0 COMPLETE: FastEmbed BM25 deployed (both images rebuilt with baked `/app/models` HF-cache model, `local_files_only=True` — zero runtime network), Firestore native DB created in asia-south1 (was missing — caused Pub/Sub poison-pill loop; fixed with `set(merge=True)` progress writes), Qdrant delete race fixed (`wait=True`). Wiped + re-ingested all 8 golden docs cleanly (doc_007 finally ingested: 882 chunks/84 pages). Eval Phase 2.5: Recall@5 0.840, Page-Recall 0.280, MRR 0.251, tenant isolation pass, VLM sweep best 97.5% (cov=0.40). Latency disambiguation: `/search` warm ~3s (Vertex embed dominated, Qdrant itself 0.04s), cold start ~14s (retrieval-api scales to zero — no minScale). `/query` grounding verified: real citations + bbox, no `[SOURCE_NN]` hallucination; off-by-one page/wrong-doc gaps noted (aligns with low page-recall). Committed as 3f43e08. Next: Phase 4.0 Auth & Multi-Tenant Security (awaiting go-ahead).
- 2026-08-17 · Command Code · Phase 3.0 finalization: tuned Cloud Run scaling — ingestion-worker `min-instances` 1→0 (saves idle cost, background cold starts OK), retrieval-api `min-instances` 0→1 (kills ~14s chat cold starts, ~₹2,500/mo). Both `deploy.sh` and `.github/workflows/ci.yml` updated in lockstep (CI redeploys on main push). Fixed eval latency math: Test 2-A now reports server-side `latency_ms` from the response instead of client wall-clock (removes the per-query `gcloud auth` subprocess overhead — reported latency should now be ~3s, not ~8.7s). Baseline MVP metrics logged: Recall 0.84, MRR 0.251. Committed. Next: Phase 4.0 Auth & Multi-Tenant Security.
- 2026-08-17 · Antigravity · Phase 3.5 COMPLETE: strict page-boundary chunking implemented via parser charspan split, eval harness updated to server latency_ms + token cache, Cloud Run scaling active. Re-ingested all 8 docs and ran golden eval: Recall@5 0.830, Page-Recall@5 0.320 (+14% lift), MRR 0.262, server latency P95 2.9s. Next: Phase 4.0 Auth & Multi-Tenant Security.
- 2026-08-19 · Command Code · Phase 4.0 COMPLETE: `services/common/auth/` (JWT verify, claims, rate limit, ID validation), per-route Firebase auth on retrieval-api, strict tenant rewrite (anti-IDOR), `firestore.rules` deployed + emulator-tested (10/10), session CRUD + cascading deletes, signed GCS URLs (IAM signer fix for Cloud Run metadata creds), `X-Firebase-Token` header (Cloud Run platform rejects Firebase JWTs in `Authorization`). Deployed retrieval-api `--allow-unauthenticated` (app enforces JWT); ingestion-worker stays Cloud Run IAM. Renamed `/healthz`→`/livez` (Cloud Run platform intercepts `/healthz`). All 4 live penetration tests PASS: 4-A cross-tenant rewrite (spoofed header/body ignored), 4-B Firestore rules (5/5), 4-C signed URL (900s TTL, cross-tenant 404), 4-D rate limit (429). 186 local tests green. Next: Phase 5.0 Frontend Integration.
- 2026-08-21 · Command Code · Phase 5.8 (Vercel deploy) progress: created `naturepivot/iris-frontend` Vercel project (team naturepivot, project `prj_co5vP2reLCZq7rPd9NCCSgC6ETh0`), set 7 production env vars (NEXT_PUBLIC_API_URL + Firebase web config), production deploy LIVE at `https://iris-frontend-steel-nine.vercel.app` (build green, `/sign-in` 200). Added `iris.procambrian.ai` to the project. **Backend:** CORS expanded on retrieval-api via `gcloud run services update` (revision `retrieval-api-00022-r84`, 100% traffic) to `https://procambrian.ai,http://localhost:3000,https://iris.procambrian.ai,https://iris-frontend-steel-nine.vercel.app`. Preflight verified 200 + ACAO/ACAH from the .vercel.app origin. Gotchas hit: `--set-env-vars` on Windows chokes on `http://localhost:3000` (Bad syntax for dict arg) — must use `--env-vars-file`; and `--env-vars-file` REPLACES all env vars (dropping MODEL_BACKEND/QDRANT_URL crashed the first revision — re-deployed with full env set, no outage since Cloud Run kept the last healthy revision). **Next / BLOCKED on user:** add CNAME `iris → b248b66e451f2298.vercel-dns-017.com` at Hostinger (down for maintenance at the time) so `https://iris.procambrian.ai` resolves; then end-to-end verify on the live domain + 5-B cross-browser + 5-C load test.
- 2026-08-22 · Command Code · Retrieval-quality round (Stages 0-5 of the quality plan, all GCP-tested live): query embeddings switched to task_type=RETRIEVAL_QUERY + cached embed model (P95 2.9s→426ms), diversity pass page-keyed + doc-scoped skip, bbox union-direction/prov-union fixes verified live (100/100 valid TOPLEFT bboxes), 256-token chunks (digital text only — corpus is VLM-chunk dominated), Hindi BM25 preprocessing (env-gated, re-ingest #2). **Live eval: Recall@5 0.88, Page-Recall@5 0.384 (was 0.320), MRR 0.299 (was 0.262), P95 426ms (<500 target).** hindi_lookup page-recall 0.333→1.0. Reranker: real Ranking API wired (semantic-ranker@latest, discoveryengine API enabled + `roles/discoveryengine.viewer` on retrieval-api-sa, 3500-char records) but live sweep shows no lift on this corpus (best +0.003 MRR at ~700ms cost) — RERANK_BLEND stays off, available for future corpora. Gotchas: VLM chunks (vlm_full_page/vlm_table, one 131K table) bypass CHUNK_TARGET_TOKENS by design — scanned_lookup page-recall (0.143) needs VLM-output chunking to move; eval harness now retries through the 30/min tenant limiter; upload-trigger 429s (Vertex trial quota) recovered via direct worker /ingest retrigger (scripts/round_a_recover.py). Pending: CI env parity for new vars before pushing main; frontend highlight ladder (S4); Phase 6.1-6.4 (S6).
- 2026-08-23 · Command Code · Measurement overhaul + page-level VLM fallback LIVE. (1) Golden-label corruption fixed in 3 rounds: original labels used the docs' PRINTED page numbers (per-doc, non-uniform offsets — doc_008 printed=viewer+2, doc_007 mixed) while pipeline/citations/viewer use PDF physical sequence; ~30 queries corrected incl. wrong answers (q_005, q_009, q_029 MP-vs-Maharashtra). (2) Silent data loss found+fixed: Docling detects zero layout elements on some scanned pages (doc_001 1/7 indexed, doc_002 2/7, doc_008 12/14, doc_003 p27 digital) → pages ingested with chunks=0. Fallback in ingest() (empty-text full-page element → router's low-text path → VLM OCR; log zero_element_page_fallback). Re-ingested: **201/201 pages (100%), 1,379 chunks. (3) Eval on complete corpus: Recall@5 1.000, Page-Recall@5 0.740, MRR 0.667** — scanned_lookup page-recall 0.143→0.857. Weak cells now page-precision: short_ambiguous 0.47, hindi 0.67, multi_hop 0.63 → VLM chunking is the next lever. (4) Canary deployed (Function+Scheduler 15min, 5 assertions incl. direct Ranking API probe; hardened with warm-up after cold-start delta false-negatives). **GOTCHA: GitHub Actions CI has stalled — last 3 pushes built nothing; worker deployed manually (rev 00080). Check the Actions tab (WIF auth/quota?).** Label conventions + floors-not-finals rules in label_adjudication_guide.md.

---

## 5. Gotchas & Notes

- Service Account Creation: `iris-backend-sa` created under Application data. Granting project-level IAM roles (Vertex AI User, Storage Admin, Datastore User) requires `resourcemanager.projects.setIamPolicy` permission or can be assigned via GCP project IAM settings / local `gcloud auth application-default login`.
- Docling emits normalized `[left, top, right, bottom]` bbox per element — verify coordinate space against PDF.js rendering in Phase 5.0.
- Phase numbering in ACTIONPLAN has a known quirk: Phase 15.0 and 16.0 reuse task numbers (10.x / 11.x) — don't let that confuse references.
- Chunking target: ~512 tokens, sentence-boundary; VLM outputs become single chunks with the source element's bbox.
- `is_tenant=True` (ACTIONPLAN Task 2.2) is **not a real Qdrant parameter**. Real tenant-filter performance comes from `payload_m` in HNSW config + a `tenant_id` keyword payload index + mandatory tenant filter on every query. Phase 2.0 must use these instead.
- VLM Router ordering: Table/Picture elements route to Gemini Vision **before** the <150-char low-text check — structural elements always go to VLM regardless of char count (fixed in `vlm_router.py`; tests enforce it).
- Docling runs **inside the ingestion-worker Cloud Run container** (CPU + onnxruntime), not on the laptop; the Dockerfile must warm the model cache at build time so cold starts don't re-download weights.
- Latency budgets that gate acceptance: `/search` < 500ms, `/query` < 2s, rewrite step < 300ms, HyDE < 400ms, graph traversal < 300ms.
- Kill switch does NOT set `max-instances=0` — Cloud Run rejects it (Knative requires positive integer). Instead, detaches the push endpoint from the ingestion subscription (`pushConfig={}`) to halt new deliveries while preserving existing chunks.
- `gcloud` CLI is NOT available in the Cloud Functions gen2 Python runtime. The kill switch uses `google-cloud-pubsub` Python client instead (`google.pubsub_v1.types.PushConfig` + `google.protobuf.field_mask_pb2.FieldMask`).
- Billing budget→topic connection is Console-only: `billing-<ID>@billing.gserviceaccount.com` SA never exists to bind programmatically; the real SA is `billing-budget-alert@system.gserviceaccount.com`, auto-created by the Console flow.
- Org policy `iam.allowedPolicyMemberDomains` required a project-level ALLOW override on `naturepivot-rag` (to allow the Google billing SA). The clean org-level fix (appending `gserviceaccount.com`) was rejected — the policy uses encoded customer IDs (`C02t34gra` format), not domain strings.
- Vertex AI API quota caps (to be set once billing account is upgraded from free trial to pay-as-you-go): `generate_content_requests_per_minute` = 15, `online_prediction_requests_per_minute` = 60. Free trial / promotional credit accounts cannot manually adjust API quotas — the Console sliders are locked until the billing account is upgraded. Once upgraded, set these in Console at APIs & Services → Vertex AI API → Quotas. If ingestion starts returning `429 RESOURCE_EXHAUSTED` before the caps are set, it means Vertex AI is being called faster than the default trial limits allow.
- Cloud Run platform intercepts the `/healthz` path and returns a Google-fronted 404 before it reaches the container — health checks must use a non-reserved path (renamed to `/livez` on both services).
- Cloud Run's platform validates any `Authorization: Bearer` token as a Google OIDC token. A Firebase user JWT in `Authorization` gets rejected with platform 401 before reaching the app — send Firebase ID tokens in `X-Firebase-Token` (the app accepts both; `Authorization: Bearer` still works for Google-issued tokens / non-Cloud-Run contexts).
- `--allow-unauthenticated` on Cloud Run allows requests with NO token; a request WITH an unverifiable Bearer token still gets platform 401. This is why retrieval-api (public, app-enforced JWT) needs the custom header path while ingestion-worker (machine-only) stays `--no-allow-unauthenticated`.
- `blob.generate_signed_url()` on Cloud Run fails with `AttributeError: you need a private key to sign credentials` — compute-engine metadata credentials have no private key. Fix: build a `Signing` credential from `google.auth.iam.Signer` + `google.oauth2.service_account.Credentials(signer=...)` (retrieval-api SA self-binds `roles/iam.serviceAccountTokenCreator`). Do not use the legacy `storage._signing.IAMSigner` — it was removed in google-cloud-storage 3.x.
- Firebase Admin SA (`firebase-adminsdk-fbsvc@...`) key creation is blocked by org policy `iam.disableServiceAccountKeyCreation`; to run admin operations (set custom claims, provision users) impersonate it with `gcloud auth print-access-token --impersonate-service-account=firebase-adminsdk-fbsvc@...` (requires `roles/iam.serviceAccountTokenCreator` + `roles/iam.serviceAccountUser` on it — grant temporarily, remove after).
- The eval harness (`scripts/eval_phase2.py`) needs a provisioned Firebase user with `tenant_id` claims; `scripts/setup_firebase.sh` now provisions `eval@iris.local` automatically via `scripts/provision_eval_user.py`. Password via `EVAL_USER_PASSWORD` env (default `EvalPass!2026x`).
- 2026-08-20 · Command Code · Phase 5.0a CORS implemented (not yet deployed): `CORSMiddleware` added to `retrieval-api` behind `CORS_ALLOWED_ORIGINS` env var (comma-separated, read at import via `add_cors_middleware()` helper; no-op when unset), wired into `scripts/deploy.sh` and `.github/workflows/ci.yml` (env-var parity), new `tests/test_cors.py` (5 tests: allowlisted-origin preflight 200, `X-Firebase-Token` in `access-control-allow-headers`, unknown origin blocked, no-op without env var, authed request unaffected). Plan saved to `~/.commandcode/plans/cors-retrieval-api.md`. **Remaining:** set the real Vercel origin in `CORS_ALLOWED_ORIGINS` (deploy.sh / CI var), deploy retrieval-api, verify live preflight. Then Phase 5.0b (`POST /documents/upload`) and the walking-skeleton core loop.
- 2026-08-20 · Command Code · Phase 5.0a CORS DEPLOYED: origin decided `https://procambrian.ai,http://localhost:3000` (custom domain + local dev; `iris.procambrian.ai` subdomain to be added in Task 5.8). Deployed via direct gcloud (Windows has no working bash — `deploy.sh`'s bash failed silently; ran `gcloud builds submit` + `gcloud run deploy` manually with identical flags). retrieval-api revision `retrieval-api-00018-mwv` live. **Live preflight verified:** `OPTIONS /query` from `https://procambrian.ai` → 200 + `access-control-allow-origin` + `access-control-allow-headers: content-type,x-firebase-token` + credentials true; from `http://localhost:3000` → 200; from `https://evil.example` → 400 (no allow-origin header). **Remaining:** set GitHub Actions repo var `CORS_ALLOWED_ORIGINS` (so CI deploys keep parity; not set yet — needs `gh` or GitHub UI), then the walking-skeleton core loop (5.1–5.3, 5.5, 5.6).

## 6. Open Questions

- **PENDING ACTION (Phase 5.8, blocked on user):** add CNAME `iris → b248b66e451f2298.vercel-dns-017.com` at the procambrian.ai DNS host (Hostinger — was down for maintenance 2026-08-21). Once added + propagated, `vercel domains verify iris.procambrian.ai` should pass and `https://iris.procambrian.ai` serves the deployed frontend. Frontend is already live meanwhile at `https://iris-frontend-steel-nine.vercel.app` (backend CORS already allows it).
- When to actually start Phase 0.0 (needs GCP account access + the $300 credit org).
- Trial/freemium credit model numbers (1 page ≈ 5 credits, 1 query ≈ 1 credit are placeholders).
- Whether Deep Search should be enabled at MVP or held for Phase 8.0+.
