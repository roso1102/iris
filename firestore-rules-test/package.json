{
  "name": "iris-firestore-rules-test",
  "version": "1.0.0",
  "private": true,
  "description": "Local Firestore security-rules tests for IRIS Phase 4.0 (Firebase Emulator)",
  "scripts": {
    "test": "mocha --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.0.0",
    "mocha": "^10.8.2"
  }
}
