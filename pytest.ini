[pytest]
testpaths = tests
markers =
    live: tests that hit live GCP APIs (Vertex AI, Qdrant) — real cost, run rarely
    integration: heavyweight tests needing real Docling models / PDFs — minutes, not seconds
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
