# IRIS — shared auth (Phase 4.0).
# Firebase JWT verification, tenant-scoped auth context, ID validation,
# and per-tenant rate limiting. Used by retrieval-api (FastAPI) and
# ingestion-worker QA view (Flask).
