# IRIS — Ingestion Worker runtime dependencies (Phase 1.0).

flask==3.*
pydantic==2.*
google-cloud-storage==3.*
google-cloud-pubsub==2.*
google-cloud-run==0.16.*
google-cloud-firestore>=2.0

# Layout-aware parsing (ACTIONPLAN 1.4) — CPU-only, runs inside the container.
docling==2.*
# Page rendering for VLM router crops (PyMuPDF).
pymupdf==1.*
# Chunk store write path (ACTIONPLAN 1.9) — gated behind QDRANT_URL (Phase 2.0).
qdrant-client==1.13.0
# BM25 sparse-vector generation during ingestion (Phase 2.0 upsert_batch).
fastembed==0.8.0
rank-bm25==0.2.*

# Preflight payload scanner (ACTIONPLAN 1.2)
pypdf==5.*

# Vertex AI SDK for Gemini Vision + text embeddings (ACTIONPLAN 1.6-1.7).
google-cloud-aiplatform[vertexai]==1.*
